# 平台数据中心 — 分阶段完成事项与路径约束

版本：v1.0
定位：分阶段推进的项目执行指南
目标：明确各阶段的交付物边界、规范约束和实施路径

---

## 0. 本指南的用途

顶层架构文档定义了"做什么"和"按什么流程做"，本指南定义：

- **每个阶段做什么** — 交付物清单
- **走哪条路去做** — 目录结构、文件命名、代码组织等路径约束
- **做到什么程度为止** — 阶段验收标准

本指南按阶段细化，每个阶段都定义了一个可独立验证的子集。阶段之间无强依赖，可并行推进。

---

## 1. 总交付物地图

```
阶段             核心交付                         依赖的 Hook         验证方式
─────────────────────────────────────────────────────────────────────────────
Phase 1          核心流程闭环                      全部核心 Hook       端到端流程跑通
  ├ 骨架搭建      Hook 接口定义 + 工厂函数签名      -                  编译通过
  ├ Mock 实现     DatabaseAccessor/AccountManager   MySQL(mock)        单表可运行
  ├ 业务层        流程编排 + 校验                   Mock Hook          流程跑通
  └ 真实实现      MySQL DatabaseAccessor +          MySQL 8.4          DDL 生效
                  账号管理实现
─────────────────────────────────────────────────────────────────────────────
Phase 2          完整 CRUD + 查询功能               Drizzle ORM        元数据 CRUD 测试
─────────────────────────────────────────────────────────────────────────────
Phase 3          安全 + 可观测性                   Logger/AuthGuard   安全测试
─────────────────────────────────────────────────────────────────────────────
Phase 4          声明生命周期增强                   -                  增量测试
```

---

## 2. Phase 1 — 核心流程闭环

### 2.1 目标

跑通从"创建平台 → 创建资源 → 提交声明 → 建库建表 → 建账号 → 返回凭证"的完整闭环。

中间所有底层实现（数据库操作、账号管理）先用 Mock 替代，真实实现并行开发。业务逻辑层不感知 Mock 与否。

### 2.2 交付物清单

| # | 交付物 | 类型 | 说明 |
|---|--------|------|------|
| 1 | `src/hooks/types.ts` | 接口文件 | 所有 Hook 的 TypeScript interface |
| 2 | `src/hooks/mock/*` | 实现 | DatabaseAccessor / AccountManager 的 Mock 实现 |
| 3 | `src/core/schema-handler.ts` | 业务逻辑 | PUT /schema 的业务编排 |
| 4 | `src/app/api/v1/meta/platform*/route.ts` | 路由 | 平台 CRUD |
| 5 | `src/app/api/v1/meta/user*/route.ts` | 路由 | 用户 CRUD |
| 6 | `src/app/api/v1/meta/database-resource*/route.ts` | 路由 | 数据库资源 CRUD |
| 7 | `src/app/api/v1/database-resources/[id]/schema/route.ts` | 路由 | 结构声明接口 |
| 8 | 系统数据库表（Drizzle schema） | 数据层 | 6 张系统表的 Drizzle schema 定义 |
| 9 | `tests/` | 测试 | 核心流程的端到端测试 |

### 2.3 路径约束

```
src/
├── app/
│   └── api/
│       └── v1/
│           ├── meta/
│           │   ├── platforms/          # 平台元数据
│           │   │   ├── route.ts        # POST / GET (列表)
│           │   │   └── [id]/
│           │   │       └── route.ts    # GET / PATCH / DELETE
│           │   ├── users/              # 同上模式
│           │   │   ├── route.ts
│           │   │   └── [id]/
│           │   │       └── route.ts
│           │   └── database-resources/ # 同上模式
│           │       ├── route.ts
│           │       └── [id]/
│           │           └── route.ts
│           └── database-resources/
│               └── [resourceId]/
│                   └── schema/
│                       └── route.ts    # PUT / GET
├── core/               # 业务逻辑层
│   ├── schema-handler.ts
│   ├── meta-handler.ts
│   └── types.ts        # 业务对象类型
├── hooks/              # Hook 接口 + 实现
│   ├── types.ts        # 所有 Hook 接口定义
│   └── mock/
│       ├── database-accessor.mock.ts
│       └── account-manager.mock.ts
├── db/                 # 系统数据库层（Drizzle）
│   ├── schema.ts       # 表定义
│   ├── index.ts        # 连接 + 客户端导出
│   └── migrations/     # 迁移文件
├── lib/                # 工具
│   ├── response.ts     # 统一响应辅助
│   ├── errors.ts       # 错误类型定义
│   └── validator.ts    # 通用校验
└── middleware.ts       # Next.js middleware（预留 AuthGuard）
```

### 2.4 文件命名规范

| 场景 | 命名 |
|------|------|
| 路由文件 | `route.ts`（Next.js 约定） |
| Hook 接口定义 | `types.ts` |
| Hook 实现 | `<hook-name>.<实现名>.ts`，如 `database-accessor.mock.ts` |
| 业务逻辑 | `<domain>-handler.ts`，如 `schema-handler.ts` |
| 数据层 | `schema.ts` / `index.ts` |
| 工具 | 名词式命名，如 `response.ts` / `errors.ts` |
| 测试文件 | 与被测文件同名 + `.test.ts` |

### 2.5 依赖注入规范

**强制规则：**

- 每个路由 handler 使用工厂函数接收依赖
- 工厂函数签名必须符合 `createXHandler(deps: HandlerDependencies)`
- 依赖的默认注入点在 `src/lib/container.ts`（初期可直连接口）

**模块级工厂函数模板：**

```typescript
// src/core/schema-handler.ts
import type { HandlerDependencies, SchemaDeclaration, SchemaResponse } from '@/hooks/types';

export function createSchemaHandler(deps: HandlerDependencies) {
  return {
    applySchema: async (
      resourceId: string,
      declaration: SchemaDeclaration
    ): Promise<SchemaResponse> => {
      // 业务编排 — 只调用 deps.* 上的方法
      const { databaseAccessor, accountManager } = deps;
      // ...
    },

    getSchema: async (resourceId: string) => {
      // 查询已有声明
    },
  };
}
```

```typescript
// src/app/api/v1/database-resources/[resourceId]/schema/route.ts
import { createSchemaHandler } from '@/core/schema-handler';
import { mockDatabaseAccessor } from '@/hooks/mock/database-accessor.mock';
import { mockAccountManager } from '@/hooks/mock/account-manager.mock';

const schemaHandler = createSchemaHandler({
  databaseAccessor: mockDatabaseAccessor,
  accountManager: mockAccountManager,
  // 其余支撑 Hook 使用默认实现
});

export async function PUT(req: Request, { params }: { params: { resourceId: string } }) {
  // 编排调用 schemaHandler.applySchema(...)
}
```

### 2.6 Mock 实现规范

DatabaseAccessor Mock：

```typescript
// src/hooks/mock/database-accessor.mock.ts
// 内存模拟，不涉及真实 MySQL
// createDatabase：记录 dbName 到内存 map
// executeDDL：校验 SQL 语法，记录表结构
// verifyTable：对比内存中的表结构与期望 DDL
```

AccountManager Mock：

```typescript
// src/hooks/mock/account-manager.mock.ts
// 返回固定的测试用户名和密码（不真的创建账号）
// createAccount：返回 { username: "test_user", password: "test_pass_123" }
// grantAccess：无操作，直接返回
```

支撑 Hook Mock（使用默认实现即可，无需单独编写）：

```typescript
// src/hooks/mock/defaults.ts
import crypto from 'node:crypto';
import { createHash } from 'node:crypto';
import pino from 'pino';

export const defaultIdGenerator = { generate: () => crypto.randomUUID() };
export const defaultHashComputer = { compute: (s: string) => createHash('sha256').update(s).digest('hex') };
export const defaultClock = { now: () => new Date() };
export const defaultLogger = pino({ level: 'info' });
```

### 2.7 Phase 1 验收标准

- [ ] 6 张系统表 DDL 可成功创建
- [ ] `POST /api/v1/meta/platforms` 可创建平台
- [ ] `POST /api/v1/meta/users` 可创建用户
- [ ] `POST /api/v1/meta/database-resources` 可创建数据库资源
- [ ] `PUT /api/v1/database-resources/{id}/schema` 传入合法声明 → 返回声明结果 + 连接信息
- [ ] 同一声明重复提交 → 幂等返回
- [ ] 非法声明 → 返回 `SCHEMA_INVALID`
- [ ] Mock 实现下全部流程可跑通（不依赖真实 MySQL）
- [ ] `resource_operations` 正确记录每次操作
- [ ] 单元测试覆盖核心业务逻辑分支（成功 / 失败 / 幂等）

---

## 3. Phase 2 — 完整 CRUD 与查询能力

### 3.1 目标

补齐元数据的完整 CRUD 和列表查询功能，替换 Mock Hook 为真实 MySQL 实现。

### 3.2 交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | `src/hooks/mysql/database-accessor.mysql.ts` | DatabaseAccessor 的 MySQL 实现 |
| 2 | `src/hooks/mysql/account-manager.mysql.ts` | AccountManager 的 MySQL 实现 |
| 3 | Drizzle schema 完整定义 | 6 张表的 Drizzle schema + migrations |
| 4 | CRUD 路由补齐 | PATCH、DELETE 的完整实现 |
| 5 | 列表查询 | 支持 cursor 分页、platformId/status 过滤 |
| 6 | 元数据 validation 层 | zod schema 集中管理 |
| 7 | 统一错误处理 | middleware 级别的错误捕获器 |

### 3.3 路径约束

```
src/
├── hooks/
│   └── mysql/
│       ├── database-accessor.mysql.ts
│       └── account-manager.mysql.ts
└── db/
    ├── schema/
    │   ├── platforms.ts
    │   ├── system-users.ts
    │   ├── platform-database-resources.ts
    │   ├── schema-declarations.ts
    │   ├── schema-declaration-tables.ts
    │   └── resource-operations.ts
    ├── index.ts         # 连接管理
    └── migrations/      # drizzle-kit 生成
```

### 3.4 Phase 2 验收标准

- [ ] `src/hooks/mysql/*` 实现通过数据库连接的实际集成测试
- [ ] PATCH 正确更新指定字段
- [ ] DELETE 正确执行软删除（deleted_at 写入，列表查询过滤 deleted）
- [ ] 列表查询支持 cursor 分页，且性能满足预期
- [ ] 非法入参返回 `INVALID_INPUT` + 400
- [ ] 重复创建返回 `ENTITY_CONFLICT` / `PLATFORM_KEY_EXISTS` / `RESOURCE_KEY_EXISTS`
- [ ] 请求不存在的实体返回 `ENTITY_NOT_FOUND` + 404
- [ ] 替换 Mock → MySQL 后，核心流程全覆盖验收

---

## 4. Phase 3 — 安全与可观测性

### 4.1 目标

启用 AuthGuard Hook，增加请求粒度的安全控制；完善日志和可观测性。

### 4.2 交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | AuthGuard 真实实现 | API Key 或 JWT 鉴权 |
| 2 | RateLimiter | 请求频率限制 |
| 3 | 请求日志中间件 | 每条请求的 method / path / duration / status |
| 4 | 操作审计增强 | 记录操作发起方身份 |
| 5 | 错误监控 | INTERNAL_ERROR 的告警输出 |

### 4.3 Phase 3 验收标准

- [ ] 无凭证请求被拒绝（401）
- [ ] 无效凭证被拒绝（403）
- [ ] 超频请求被限制（429）
- [ ] 每条请求都有结构化日志

---

## 5. Phase 4 — 声明生命周期增强

### 5.1 目标

支持结构声明的版本历史管理和声明回滚能力。

### 5.2 交付物清单

| # | 交付物 | 说明 |
|---|--------|------|
| 1 | 声明版本列表 | `GET .../schema/versions` 返回所有历史版本 |
| 2 | 声明回滚 | `PUT .../schema/rollback/{version}` 回退到指定版本 |

### 5.3 Phase 4 验收标准

- [ ] 多次提交生成递增的 version
- [ ] 回滚后表结构恢复到指定版本
- [ ] 回滚操作记录到 resource_operations

---

## 6. 全局编程规范

> 以下规范从 Phase 1 开始生效，贯穿所有阶段。

### 6.1 代码风格

- TypeScript strict 模式，`noUncheckedIndexedAccess` 启用
- async/await 优先，避免裸 `.then()`
- 函数参数超过 2 个时使用对象参数（`{ name, age }` 而非 `name, age`）
- 业务逻辑层不允许直接使用 `console.log`，必须通过 `deps.logger`

### 6.2 错误处理

- 业务错误使用自定义 `AppError` 类，通过 `errors.ts` 导出
- 每个 `AppError` 必须携带 `code`（对应错误码）和 `httpStatus`
- API 路由层不抛异常，catch 后统一转响应
- Hook 实现层可以抛底层错误，业务层捕获后转 `AppError`

### 6.3 测试规范

测试文件目录结构：

```
tests/
├── core/
│   └── schema-handler.test.ts     # 核心业务逻辑测试
├── hooks/
│   ├── mock/
│   │   └── database-accessor.test.ts  # Mock 自身行为验证
│   └── mysql/
│       └── database-accessor.test.ts  # MySQL 实现的集成测试
└── e2e/
    └── schema-flow.test.ts        # 端到端流程测试
```

测试基线的原则：
- 业务逻辑层测试：传 Mock 依赖，验证编排逻辑正确性
- Hook 实现测试：独立测试各实现的行为
- 集成测试：启动真实 MySQL 实例（`@databases/pg-test` 等效方案），测试全流程
