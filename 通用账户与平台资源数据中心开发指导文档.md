通用账户与平台资源数据中心第一阶段开发指导文档

版本：v1.1
定位：第一阶段技术流程跑通文档
目标：面向功能测试的最小可运行中心系统
技术基线：Next.js 16 + TypeScript + MySQL 8.4 LTS + Drizzle ORM

1. 第一阶段目标

第一阶段只跑通中心系统的技术侧流程。

系统需要具备以下能力：

中心系统维护自己的系统数据库
  ↓
中心系统记录平台元数据
  ↓
中心系统记录自然人用户元数据
  ↓
中心系统记录平台资源元数据


平台提交数据库结构声明
  ↓
中心系统根据声明创建平台业务数据库
  ↓
中心系统根据声明创建业务表结构


第一阶段完成后，应能面向功能测试证明：

可以创建平台元数据。

可以创建用户元数据。

可以创建数据库资源元数据。

系统可以根据结构声明创建平台业务数据库。

系统可以根据结构声明创建业务表。

2. 系统边界

中心系统第一阶段由以下能力组成：

系统元数据管理

数据库资源声明

声明式建库建表

资源状态记录

基础操作记录

平台业务数据库由中心系统创建和初始化结构。

平台业务数据库的实际内容、业务含义和数据读写方式由平台自行决定。

3. 正常工作流
3.1 创建平台

通过 POST /api/v1/meta/platforms 创建平台元数据。

约束：

platform key 必须符合命名规则

platform key 全局唯一

创建后平台状态初始化为 active

3.2 创建用户

通过 POST /api/v1/meta/users 创建自然人用户记录。

约束：

用户 ID 由中心系统生成

用户与平台的映射关系由平台自行维护，中心系统只提供用户数据

3.3 创建数据库资源元数据

通过 POST /api/v1/meta/database-resources 创建数据库资源记录。

约束：

platformId 必须存在

resource key 必须符合命名规则

同一平台下 resource key 唯一

database_name 由系统按命名规则自动生成

初始状态为 pending

3.4 提交并应用数据库结构声明

通过 PUT /api/v1/database-resources/{resourceId}/schema 提交结构声明并立即应用。

约束：

接收声明后系统应先确保业务数据库存在，再处理表结构

同一资源重复提交相同声明时保持幂等

详细处理规则见第 10 节。

3.5 查询当前结构声明

通过 GET /api/v1/database-resources/{resourceId}/schema 查询当前已保存的结构声明和应用状态。

4. 第一阶段接口
4.1 系统元数据接口

统一路径：

/api/v1/meta/{entity}


允许的 entity：

platforms
users
database-resources


支持方法：

POST   /api/v1/meta/{entity}
GET    /api/v1/meta/{entity}
GET    /api/v1/meta/{entity}/{id}
PATCH  /api/v1/meta/{entity}/{id}
DELETE /api/v1/meta/{entity}/{id}


用途：

platforms          管理平台元数据
users              管理自然人用户元数据
database-resources 管理平台数据库资源元数据


列表查询支持参数：

platformId
status
limit
cursor

4.2 数据库结构声明接口
PUT /api/v1/database-resources/{resourceId}/schema
GET /api/v1/database-resources/{resourceId}/schema


PUT 用于设置目标结构并立即应用。

GET 用于查询当前已保存的结构声明和应用状态。

5. 统一响应格式

所有接口统一返回结构。

成功响应必须包含：success、data、requestId。

失败响应必须包含：success、error（含 code 和 message）、requestId。

6. 系统数据库表设计
6.1 platforms

用于记录接入中心系统的网站平台。

字段	类型	说明
id	varchar(36)	主键，UUID
platform_key	varchar(64)	平台唯一 key
name	varchar(128)	平台名称
status	varchar(32)	active / disabled / deleted
created_at	datetime	创建时间
updated_at	datetime	更新时间
deleted_at		datetime	软删除时间

约束：

platform_key 唯一

6.2 system_users

用于记录自然人用户。

字段	类型	说明
id	varchar(36)	主键，UUID，同时作为全局用户 ID
status	varchar(32)	active / disabled / deleted
created_at	datetime	创建时间
updated_at	datetime	更新时间
deleted_at		datetime 	软删除时间
6.3 platform_database_resources

用于记录中心系统为平台管理的业务数据库资源。

字段	类型	说明
id	varchar(36)	主键，UUID
platform_id	varchar(36)	所属平台 ID
resource_key	varchar(64)	平台下资源 key
database_name	varchar(128)	系统生成的数据库名
status	varchar(32)	pending / creating / active / failed / deleted
current_schema_declaration_id	varchar(36) nullable	当前结构声明 ID
last_applied_hash	varchar(128) nullable	最近成功应用的声明 hash
created_at	datetime	创建时间
updated_at	datetime	更新时间
deleted_at		datetime	软删除时间

约束：

platform_id + resource_key 唯一
database_name 唯一

6.4 schema_declarations

用于保存平台提交的结构声明。

字段	类型	说明
id	varchar(36)	主键，UUID
resource_id	varchar(36)	所属数据库资源
version	int	声明版本
declaration_json	json	原始声明内容
declaration_hash	varchar(128)	声明内容 hash
status	varchar(32)	applying / applied / failed
created_at	datetime	创建时间
applied_at	datetime  应用成功时间

约束：

resource_id + version 唯一
resource_id + declaration_hash 唯一

6.5 schema_declaration_tables

用于记录单表结构声明的应用结果。

字段	类型	说明
id	varchar(36)	主键，UUID
declaration_id	varchar(36)	所属结构声明
resource_id	varchar(36)	所属数据库资源
table_name	varchar(128)	表名
table_hash	varchar(128)	单表结构 hash
status	varchar(32)	created / verified / failed
error_message	text nullable	单表错误
created_at		datetime	创建时间
updated_at	datetime	更新时间

约束：

declaration_id + table_name 唯一

6.6 resource_operations

用于记录基础资源操作结果。

字段	类型	说明
id	varchar(36)	主键，UUID
platform_id	varchar(36) nullable	平台 ID
resource_id	varchar(36) nullable	资源 ID
declaration_id	varchar(36) nullable	结构声明 ID
operation_type	varchar(64)	操作类型
status	varchar(32)	success / failed
created_at		datetime	创建时间

操作类型：

platform_created
user_created
database_resource_created
schema_declaration_submitted
database_created
table_created
table_verified
schema_apply_success
schema_apply_failed

7. 状态定义
7.1 platforms.status
active
disabled
deleted

7.2 system_users.status
active
disabled
deleted

7.3 platform_database_resources.status
pending
creating
active
failed
deleted

7.4 schema_declarations.status
applying
applied
failed

7.5 schema_declaration_tables.status
created
verified
failed

8. 统一命名规则：^[a-z][a-z0-9_]{0,62}[a-z0-9]$

9. 结构声明格式
9.1 顶层格式
字段	必填	说明
tables	是	表结构声明数组
9.2 表声明
字段	必填	说明
name	是	表名
columns	是	字段声明数组
indexes	否	索引声明数组
9.3 字段声明
字段	必填	默认值	说明
name	是	-	字段名
type	是	-	字段类型
nullable	否	false	是否允许 null
primary	否	false	是否主键
unique	否	false	是否唯一
default	否	无	默认值

第一阶段支持类型：

varchar(n)
text
int
bigint
decimal(p,s)
boolean
datetime
json

9.4 索引声明
字段	必填	默认值	说明
columns	是	-	索引字段
unique	否	false	是否唯一索引
name	否	系统生成	索引名

索引名生成规则：

idx_<table>_<column1>_<column2>

10. 结构声明应用规则

结构声明接口执行时，系统按以下规则处理：

业务数据库不存在：创建数据库

业务数据库已存在：继续处理表结构

表不存在：创建表

表存在且结构一致：标记 verified

表存在但结构不一致：标记 failed

全部表成功：资源状态设为 active

任一表失败：资源状态设为 failed

结构声明 hash 应基于规范化后的声明内容计算。

同一资源重复提交相同声明时，应返回已有结果或重新校验后返回成功。

11. 错误码

第一阶段错误码保持最小集合：

INVALID_INPUT
ENTITY_NOT_FOUND
ENTITY_CONFLICT
PLATFORM_KEY_EXISTS
RESOURCE_KEY_EXISTS
DATABASE_CREATE_FAILED
SCHEMA_INVALID
SCHEMA_MISMATCH
SCHEMA_APPLY_FAILED
UNAUTHORIZED
INTERNAL_ERROR

12. 技术约束

类别	技术
Web 框架	Next.js 16 App Router
语言	TypeScript
数据库	MySQL 8.4 LTS
ORM	Drizzle ORM
MySQL 驱动	mysql2
入参校验	zod
服务端日志	pino
测试	vitest
包管理	pnpm
代码格式	eslint + prettier