# 通用账户与平台资源数据中心顶层架构文档

版本：v2.0
定位：顶层架构设计文档
技术基线：Next.js 16 + TypeScript + MySQL 8.4 LTS + Drizzle ORM

---

## 0. 项目架构思路

本系统采用 **Hook 模式** 设计。将"可替换的底层实现"抽象为接口合约（Hook），核心业务逻辑只依赖合约签名，不依赖具体实现。依赖通过**模块级工厂函数**注入（`createHandler(deps)`），业务逻辑与基础设施完全解耦。

### 分层结构

```
                         ┌──────────────────────┐
                         │   API 路由层           │
                         │  请求接收 / 响应编排    │
                         ├──────────────────────┤
                         │   业务逻辑层            │
                         │  流程编排 + 校验        │
                         ├──────────────────────┤
                         │   Hook 接口层           │
                         │  抽象合约定义            │
                         ├──────────────────────┤
                         │    实现层（可替换）      │
                         │  mock / mysql / ...    │
                         └──────────────────────┘
```

### 核心原则

- 业务逻辑不直接依赖数据库驱动、账号管理等具体实现
- 所有对外副作用都通过 Hook 接口调用
- Hook 以模块级工厂函数注入：`createHandler({ databaseAccessor: …, accountManager: … })`
- 测试时传入 Mock 实现即可验证核心流程正确性
- 底层子系统可并行开发，开发完成后替换 Hook 实现即可

### 第一阶段的开发顺序

1. 定义 Hook 接口合约（TypeScript `interface`）
2. 编写核心业务逻辑（编排 Hook 调用 — 校验 → 建库 → 建表 → 建账号 → 返回凭证）
3. 实现 Mock DatabaseAccessor 和 AccountManager — 流程即刻可跑通
4. 后续 MySQL 真实实现放入 Phase 2，完成后替换 Mock

---

## 1. Hook 架构总览

### 1.1 核心 Hook（必需实现，Phase 1 必须就绪）

| Hook | 方法 | 说明 |
|------|------|------|
| `DatabaseAccessor` | `createDatabase(dbName)` | 创建业务数据库 |
| | `executeDDL(dbName, statements[])` | 在业务库执行 DDL |
| | `verifyTable(dbName, tableName, tableDDL)` | 校验单表结构一致性 |
| `AccountManager` | `createAccount(platformKey): { username, password }` | 创建数据库账号 |
| | `grantAccess(username, dbName)` | 授权账号访问业务库 |

### 1.2 支撑 Hook（提供默认实现，可替换）

| Hook | 默认实现 | 说明 |
|------|----------|------|
| `IdGenerator` | `crypto.randomUUID` | UUID 生成 |
| `HashComputer` | SHA256 | 声明内容 hash 计算 |
| `Clock` | `new Date()` | 时间获取 |
| `Logger` | pino console | 日志输出 |

### 1.3 预留 Hook（当前透传，Phase 3 启用）

| Hook | 当前行为 |
|------|----------|
| `AuthGuard` | 恒返回 `true`，不拦截 |
| `RateLimiter` | 不限制 |

---

## 2. 第一阶段目标

第一阶段只跑通中心系统的技术侧流程。

系统需要具备以下能力：

```
中心系统维护自己的系统数据库
  ↓
中心系统记录平台元数据
  ↓
中心系统记录自然人用户元数据
  ↓
中心系统记录平台资源元数据

平台提交数据库结构声明
  ↓
中心系统根据声明创建平台业务数据库
  ↓
中心系统根据声明创建业务表结构
  ↓
中心系统创建数据库账号并返回连接凭证
```

第一阶段完成后，应能面向功能测试证明：

- 可以创建平台元数据。
- 可以创建用户元数据。
- 可以创建数据库资源元数据。
- 系统可以根据结构声明创建平台业务数据库。
- 系统可以根据结构声明创建业务表。
- 系统可以创建数据库账号并返回连接信息。
- 重复提交相同声明时保持幂等。

---

## 3. 系统边界

中心系统第一阶段由以下能力组成：

- 系统元数据管理
- 数据库资源声明
- 声明式建库建表
- 数据库账号创建与授权（返回凭证，不存储）
- 资源状态记录
- 基础操作记录

中心系统的职责边界：

- 业务数据库由中心系统创建和初始化结构
- 数据库账号由中心系统创建，授权到业务库
- 密码仅创建时返回一次，中心系统不存储
- 业务数据库的实际内容、业务含义和数据读写方式由平台自行决定
- 平台拿到凭证后直接连接业务数据库，业务请求不经过中心系统

---

## 4. 正常工作流

### 4.1 创建平台

通过 `POST /api/v1/meta/platforms` 创建平台元数据。

约束：
- `platform_key` 必须符合命名规则
- `platform_key` 全局唯一
- 创建后平台状态初始化为 `active`

### 4.2 创建用户

通过 `POST /api/v1/meta/users` 创建自然人用户记录。

约束：
- 用户 ID 由中心系统生成
- 用户与平台的映射关系由平台自行维护，中心系统只提供用户数据

### 4.3 创建数据库资源元数据

通过 `POST /api/v1/meta/database-resources` 创建数据库资源记录。

约束：
- `platformId` 必须存在
- `resource_key` 必须符合命名规则
- 同一平台下 `resource_key` 唯一
- `database_name` 由系统自动生成（规则待定：等开发数据库访问器模块时决定）
- 初始状态为 `pending`

### 4.4 提交并应用数据库结构声明

通过 `PUT /api/v1/database-resources/{resourceId}/schema` 提交结构声明并立即应用。

完整执行流程：

```
① 校验结构声明格式（zod schema）
② 检查资源状态（必须为 pending / active / failed）
③ Lock resource（防止并发冲突）
   ↓
④ DatabaseAccessor.createDatabase(dbName)
   ↓ 失败 → 标记 failed，写入操作记录
⑤ DatabaseAccessor.executeDDL(dbName, tableStatements[])
   ↓ 任一表失败 → 标记 failed，已成功表保留记录
⑥ AccountManager.createAccount(platformKey)
   ↓ 失败 → 清理已建表
⑦ AccountManager.grantAccess(username, dbName)
   ↓ 失败 → 清除账号 → 清理已建表
   ↓
⑧ 写入 schema_declaration 记录 + schema_declaration_tables 记录
⑨ 更新资源状态为 active
⑩ 返回声明结果 + 数据库连接信息
```

约束：
- 同一资源重复提交相同声明时保持幂等（通过 `resource_id + declaration_hash` 唯一判定），返回已有的声明结果
- 任一步骤失败，皆须写 `resource_operations` 记录
- 全部通过后，资源状态设为 `active`；任一表失败，资源状态设为 `failed`

### 4.5 查询当前结构声明

通过 `GET /api/v1/database-resources/{resourceId}/schema` 查询当前已保存的结构声明和应用状态。

---

## 5. 第一阶段接口

### 5.1 系统元数据接口

统一路径：`/api/v1/meta/{entity}`

允许的 `entity`：`platforms` | `users` | `database-resources`

| 方法 | 路径 | 用途 |
|------|------|------|
| `POST` | `/api/v1/meta/{entity}` | 创建实体 |
| `GET` | `/api/v1/meta/{entity}` | 列表查询 |
| `GET` | `/api/v1/meta/{entity}/{id}` | 查询单个实体 |
| `PATCH` | `/api/v1/meta/{entity}/{id}` | 更新实体 |
| `DELETE` | `/api/v1/meta/{entity}/{id}` | 软删除实体 |

列表查询支持参数：`platformId`、`status`、`limit`、`cursor`

### 5.2 数据库结构声明接口

| 方法 | 路径 | 用途 |
|------|------|------|
| `PUT` | `/api/v1/database-resources/{resourceId}/schema` | 提交结构声明并立即执行 |
| `GET` | `/api/v1/database-resources/{resourceId}/schema` | 查询当前声明和应用状态 |

`PUT` 成功响应结构：

```json
{
  "success": true,
  "data": {
    "declaration": {
      "id": "",
      "version": 1,
      "hash": "",
      "status": "applied",
      "tables": [
        { "name": "", "status": "created", "hash": "" }
      ]
    },
    "database": {
      "host": "",
      "port": 3306,
      "databaseName": "",
      "username": "",
      "password": ""
    }
  },
  "requestId": ""
}
```

> `database` 字段仅在首次成功创建时返回。重复提交相同声明时，只返回 `declaration` 部分。
> `password` 仅在此响应中出现一次，系统不持久化存储。

---

## 6. 统一响应格式

所有接口统一返回结构。

**成功响应：**

```json
{
  "success": true,
  "data": { … },
  "requestId": ""
}
```

**失败响应：**

```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": ""
  },
  "requestId": ""
}
```

**HTTP 状态码映射：**

| 错误码 | HTTP Status |
|--------|-------------|
| `INVALID_INPUT` | 400 |
| `ENTITY_NOT_FOUND` | 404 |
| `ENTITY_CONFLICT` | 409 |
| `PLATFORM_KEY_EXISTS` | 409 |
| `RESOURCE_KEY_EXISTS` | 409 |
| `DATABASE_CREATE_FAILED` | 500 |
| `SCHEMA_INVALID` | 400 |
| `SCHEMA_MISMATCH` | 422 |
| `SCHEMA_APPLY_FAILED` | 500 |
| `INTERNAL_ERROR` | 500 |

---

## 7. 系统数据库表设计

### 7.1 platforms

用于记录接入中心系统的网站平台。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `varchar(36)` PK | UUID |
| `platform_key` | `varchar(64)` | 平台唯一 key |
| `name` | `varchar(128)` | 平台名称 |
| `status` | `varchar(32)` | `active / disabled / deleted` |
| `created_at` | `datetime` | |
| `updated_at` | `datetime` | |
| `deleted_at` | `datetime` nullable | |

约束：`platform_key` 唯一

### 7.2 system_users

用于记录自然人用户。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `varchar(36)` PK | UUID，同时作为全局用户 ID |
| `status` | `varchar(32)` | `active / disabled / deleted` |
| `created_at` | `datetime` | |
| `updated_at` | `datetime` | |
| `deleted_at` | `datetime` nullable | |

### 7.3 platform_database_resources

用于记录中心系统为平台管理的业务数据库资源。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `varchar(36)` PK | UUID |
| `platform_id` | `varchar(36)` | 所属平台 ID |
| `resource_key` | `varchar(64)` | 平台下资源 key |
| `database_name` | `varchar(128)` | 系统生成的数据库名 |
| `status` | `varchar(32)` | `pending / creating / active / failed / deleted` |
| `current_schema_declaration_id` | `varchar(36)` nullable | 当前结构声明 ID |
| `last_applied_hash` | `varchar(128)` nullable | 最近成功应用的声明 hash |
| `created_at` | `datetime` | |
| `updated_at` | `datetime` | |
| `deleted_at` | `datetime` nullable | |

约束：
- `platform_id + resource_key` 唯一
- `database_name` 唯一

### 7.4 schema_declarations

用于保存平台提交的结构声明。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `varchar(36)` PK | UUID |
| `resource_id` | `varchar(36)` | 所属数据库资源 |
| `version` | `int` | 声明版本（每次提交 +1） |
| `declaration_json` | `json` | 原始声明内容 |
| `declaration_hash` | `varchar(128)` | 声明内容规范化后 hash（SHA256） |
| `status` | `varchar(32)` | `applying / applied / failed` |
| `created_at` | `datetime` | |
| `applied_at` | `datetime` nullable | 应用成功时间 |

约束：
- `resource_id + version` 唯一
- `resource_id + declaration_hash` 唯一

### 7.5 schema_declaration_tables

用于记录单表结构声明的应用结果。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `varchar(36)` PK | UUID |
| `declaration_id` | `varchar(36)` | 所属结构声明 |
| `resource_id` | `varchar(36)` | 所属数据库资源 |
| `table_name` | `varchar(128)` | 表名 |
| `table_hash` | `varchar(128)` | 单表结构 hash |
| `status` | `varchar(32)` | `created / verified / failed` |
| `error_message` | `text` nullable | 单表错误信息 |
| `created_at` | `datetime` | |
| `updated_at` | `datetime` | |

约束：`declaration_id + table_name` 唯一

### 7.6 resource_operations

用于记录基础资源操作结果。

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | `varchar(36)` PK | UUID |
| `platform_id` | `varchar(36)` nullable | 平台 ID |
| `resource_id` | `varchar(36)` nullable | 资源 ID |
| `declaration_id` | `varchar(36)` nullable | 结构声明 ID |
| `operation_type` | `varchar(64)` | 操作类型 |
| `status` | `varchar(32)` | `success / failed` |
| `created_at` | `datetime` | |

操作类型枚举：
- `platform_created` / `user_created` / `database_resource_created`
- `schema_declaration_submitted` / `database_created` / `table_created`
- `table_verified` / `schema_apply_success` / `schema_apply_failed`

---

## 8. 状态定义

### 8.1 platforms.status

`active` → `disabled` → `deleted`

### 8.2 system_users.status

`active` → `disabled` → `deleted`

### 8.3 platform_database_resources.status

`pending` → `creating` → `active` → `deleted`
`pending` / `creating` / `active` → `failed`

### 8.4 schema_declarations.status

`applying` → `applied` / `failed`

### 8.5 schema_declaration_tables.status

`created` / `verified` / `failed`

---

## 9. 统一命名规则

正则：`^[a-z][a-z0-9_]{0,62}[a-z0-9]$`

适用于：`platform_key`、`resource_key`、表名、字段名、索引名

> `database_name` 生成规则待定，等开发数据库访问器模块时决定。

---

## 10. 结构声明格式

### 10.1 顶层格式

| 字段 | 必填 | 说明 |
|------|------|------|
| `tables` | 是 | 表结构声明数组 |

### 10.2 表声明

| 字段 | 必填 | 说明 |
|------|------|------|
| `name` | 是 | 表名 |
| `columns` | 是 | 字段声明数组 |
| `indexes` | 否 | 索引声明数组 |

### 10.3 字段声明

| 字段 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `name` | 是 | - | 字段名 |
| `type` | 是 | - | 字段类型 |
| `nullable` | 否 | `false` | 是否允许 null |
| `primary` | 否 | `false` | 是否主键 |
| `unique` | 否 | `false` | 是否唯一 |
| `default` | 否 | 无 | 默认值 |

第一阶段支持的类型：`varchar(n)`、`text`、`int`、`bigint`、`decimal(p,s)`、`boolean`、`datetime`、`json`

### 10.4 索引声明

| 字段 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `columns` | 是 | - | 索引字段数组 |
| `unique` | 否 | `false` | 是否唯一索引 |
| `name` | 否 | 系统生成 | 索引名 |

索引名生成规则：`idx_<table>_<column1>_<column2>`

---

## 11. 结构声明应用规则

结构声明接口执行时，系统按以下规则处理：

1. **业务数据库不存在** → `DatabaseAccessor.createDatabase(dbName)`
2. **业务数据库已存在** → 继续处理表结构
3. **表不存在** → `DatabaseAccessor.executeDDL(dbName, createTableStatement)`
4. **表存在且结构一致** → `DatabaseAccessor.verifyTable(dbName, tableName, tableDDL)` 通过，标记 `verified`
5. **表存在但结构不一致** → 标记 `failed`，记录错误信息
6. **全部表成功** → 创建账号并授权 → 资源状态设为 `active`
7. **任一表失败** → 资源状态设为 `failed`

hash 计算规则：
- 声明 hash：基于规范化后的完整声明 JSON（key 按字典序排列，去除空白差异）计算 SHA256
- 表 hash：基于单表的规范化 DDL 字符串计算 SHA256

幂等规则：
- `resource_id + declaration_hash` 已存在 → 直接返回已有声明记录
- 同一资源、不同 hash → 新建声明记录，`version` 递增

---

## 12. Hook 接口合约定义

> 以下为 TypeScript `interface` 定义，各实现模块必须遵守此契约。

```typescript
// === 核心 Hook ===

interface DatabaseAccessor {
  createDatabase(dbName: string): Promise<void>;
  executeDDL(dbName: string, statements: string[]): Promise<void>;
  verifyTable(
    dbName: string,
    tableName: string,
    tableDDL: string
  ): Promise<{ match: boolean; error?: string }>;
}

interface AccountManager {
  createAccount(platformKey: string): Promise<{
    username: string;
    password: string;
  }>;
  grantAccess(username: string, dbName: string): Promise<void>;
}

// === 支撑 Hook ===

interface IdGenerator {
  generate(): string;  // UUID
}

interface HashComputer {
  compute(content: string): string;  // SHA256 hex
}

interface Clock {
  now(): Date;
}

interface Logger {
  info(msg: string, ...args: unknown[]): void;
  error(msg: string, ...args: unknown[]): void;
  warn(msg: string, ...args: unknown[]): void;
}

// === 预留 Hook ===

interface AuthGuard {
  verify(request: Request): Promise<{ authenticated: boolean; error?: string }>;
}

// === 模块级工厂函数签名 ===

interface HandlerDependencies {
  databaseAccessor: DatabaseAccessor;
  accountManager: AccountManager;
  idGenerator?: IdGenerator;
  hashComputer?: HashComputer;
  clock?: Clock;
  logger?: Logger;
  authGuard?: AuthGuard;
}

// 示例：createSchemaHandler 的工厂函数
function createSchemaHandler(deps: HandlerDependencies) {
  return async function handlePutSchema(
    resourceId: string,
    declaration: SchemaDeclaration
  ): Promise<SchemaResponse> {
    // 业务逻辑编排，只调用 deps 上的方法
  };
}
```

---

## 13. 错误码

第一阶段错误码保持最小集合：

| 错误码 | 含义 |
|--------|------|
| `INVALID_INPUT` | 入参校验失败 |
| `ENTITY_NOT_FOUND` | 请求的实体不存在 |
| `ENTITY_CONFLICT` | 资源冲突 |
| `PLATFORM_KEY_EXISTS` | platform_key 已存在 |
| `RESOURCE_KEY_EXISTS` | 同一平台下 resource_key 已存在 |
| `DATABASE_CREATE_FAILED` | 创建业务数据库失败 |
| `SCHEMA_INVALID` | 结构声明校验不通过 |
| `SCHEMA_MISMATCH` | 表结构不一致 |
| `SCHEMA_APPLY_FAILED` | 结构声明应用失败 |
| `INTERNAL_ERROR` | 内部未预期错误 |

---

## 14. 技术约束

| 类别 | 技术 |
|------|------|
| Web 框架 | Next.js 16 App Router |
| 语言 | TypeScript |
| 数据库（中心系统） | MySQL 8.4 LTS |
| ORM | Drizzle ORM |
| MySQL 驱动 | mysql2 |
| 入参校验 | zod |
| 服务端日志 | pino |
| 测试 | vitest |
| 包管理 | pnpm |
| 代码格式 | eslint + prettier |
